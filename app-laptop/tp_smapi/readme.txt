Since 0.41, ebuild uses a new fork of tp_smapi. 

The 1st part below is the old tp_smapi which is never modified since 2008.
The 2nd part below is the URL of the new fork tp_smapi.

HOMEPAGE="http://tpctl.sourceforge.net/"
SRC_URI="mirror://sourceforge/tpctl/${P}.tgz"

HOMEPAGE="https://github.com/evgeni/tp_smapi/ http://tpctl.sourceforge.net/"
SRC_URI="mirror://github/evgeni/${PN}/${P}.tar.gz"

